apply plugin: 'java'

apply plugin: 'idea'

idea {
    module {
        sourceDirs += file('src')
        testSourceDirs += file('test')
    }
}

version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile 'commons-net:commons-net:3.3'
    testCompile 'junit:junit:4.11'
}